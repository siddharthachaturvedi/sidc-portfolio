// Main site JS (currently empty)
// Add interactive behavior here as needed.

document.addEventListener('DOMContentLoaded', () => {
  // Placeholder: site loaded
  console.log('Site scripts loaded');
});
