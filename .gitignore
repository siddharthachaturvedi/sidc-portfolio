.playwright-cli/
node_modules/
.claude/
.gemini/
.agent/
.cursor/
.antigravity/
.codex/
# Local design experiment (homepage redesign sandbox — not shipped)
experiment/

# Local harmonization plan (not shipped)
todo.md

# Parked/snapshotted code (e.g. cut terminal shell) — local only
parking-lot/

# Local font experimentation tool (dev-only)

# Local navbar design switcher (dev-only)
# Local dev-only tools (font/nav labs)
tools/dev/

# Internal audit / working doc (not shipped)
AUDIT.md
